import './App.css';
import React, { Component } from 'react';

function App() {
  
  const handleClick = () => {
    //call post command
    
  };

  return (
    <div className="App">
    <button type="button" onClick={handleClick}>Subtract Ingredient</button>
    </div>
  );
}

export default App;
